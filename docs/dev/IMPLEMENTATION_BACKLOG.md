# 実装バックログ（予定・消化・備忘）

**動作の正**はコードと `docs/dev/IMPLEMENTATION_REFERENCE.md`。本ファイルは **予定の整理・進捗メモ・外部向けに要約しやすい箇条書き**用。アルゴリズムの定義はここに書かない。

**再開時:** 作業の最初にここを開き、**予定（P0）** と「前回終わりに決めた次の一手」が一致しているかだけ見てから着手する。別件に流れる場合は、**先にこのファイルに1行メモ**してから切り替える（後で追跡できるようにする）。

**更新のしかた**

- **着手・完了・見送り**のタイミングで、**同じ作業のついでに**1行ずつ更新する（Cursor も人間も）。
- 確定した意思決定は **`docs/DECISIONS.md`**。ここは「いま何を考えているか／何が終わったか」用。

**公開に向けた方針（2026-04）** — **細かい調整より、大枠の未完了を先に**。下記は公開前の優先課題。

---

## 予定（P0・公開前に手を付けたい）

| 項目 | メモ |
|------|------|
| タイムライン **ページング／無限スクロール** | 2026-04-13 に **ページング（20件 + 追加読み込みボタン）**を実装。自動無限スクロールは未実装（必要なら IntersectionObserver で置換）。 |
| **アプリ内アクティビティ**（**新着順**・直近の反応の一覧） | 2026-04-13 に `/home/activity` を追加。自分の投稿への他者返信を新着順で表示。 |
| **新規登録の制限**（公開リポジトリ・URL 流出対策） | **未実装**。メール UI を隠す案は撤回（テストユーザのメールログインを阻害するため）。事前登録・枠配布・サーバ側の新規拒否などは要設計。メモ [`invite-media/THREADS_PHASE1_EARLY_TESTERS.md`](../invite-media/THREADS_PHASE1_EARLY_TESTERS.md)。 |

---

## 完了（新しい上）

| 日付 | 内容 |
|------|------|
| 2026-04-14 | 公開プロフィール導線を `/@handle` に移行（`/home/[uuid]` は誘導）。公開ID必須レイヤーを追加し、プロフィール編集UIを再配置（ID/ニックネーム、自己紹介カウント、外部リンク文言整理）。 |
| 2026-04-15 | 公開IDの説明文と入力規則を見直し（5〜20文字・英数字と `._-`、ユーザー向け説明から「URL」表現を避ける）。 |
| 2026-04-13 | 閲覧者設定 `toxicity_over_threshold_behavior`（`hide` / `fold`）を追加。閾値超の投稿・返信を一貫して制御。 |
| 2026-04-13 | タイムラインをページング化（20件 + 「さらに読み込む」）。初期ロードの「まだ投稿がありません」ちらつきを緩和（読み込み表示）。 |
| 2026-04-13 | `/home/activity` 追加。ホーム配下タブ（投稿 / アクティビティ）を実装。 |
| 2026-04-12 | `users` のレガシー列 `timeline_toxicity_threshold` / `reply_toxicity_threshold` を削除。閲覧フィルタは `toxicity_filter_level` のみ。`user_affinity` に中立なテーブルコメント（製品上「親密度」という語は使わない方針と整合）。 |

---

## 見送り・保留（参照用）

- タイムラインとリプで **別フィルタ強度** → `docs/DECISIONS.md`（当面は共通）。需要が見えたら列追加・リネームでよい。
